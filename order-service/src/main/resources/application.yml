server:
  port: 8083

spring:
  application:
    name: order-service

  datasource:
    url: jdbc:postgresql://localhost:5432/order_db
    username: postgres
    password: Dhoni@2060

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

services:
  product:
    url: http://localhost:8081

  customer:
    url: http://localhost:8082

management:
  endpoints:
    web:
      exposure:
        include: health,info
